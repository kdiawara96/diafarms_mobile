<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Par défaut : HTTPS obligatoire (prod, api.diafarms.com) -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Backend local en dev : IP LAN de la machine de dev (voir CORS dans
         SecurityConfiguration.java côté back) et 10.0.2.2 = alias de l'hôte
         depuis l'émulateur Android. À ajuster selon ton réseau de test. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.18</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
